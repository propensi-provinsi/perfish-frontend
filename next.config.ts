import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
  // reactCompiler: true, // disabled — causes ~5GB RAM during dev with large codebase
};

export default nextConfig;
